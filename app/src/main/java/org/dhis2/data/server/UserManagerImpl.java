package org.dhis2.data.server;

import android.content.Intent;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

import org.hisp.dhis.android.core.D2;
import org.hisp.dhis.android.core.user.User;
import org.hisp.dhis.android.core.user.UserCredentials;
import org.hisp.dhis.android.core.user.openid.IntentWithRequestCode;
import org.hisp.dhis.android.core.user.openid.OpenIDConnectConfig;

import io.reactivex.Completable;
import io.reactivex.Observable;
import io.reactivex.Single;
import kotlin.Pair;

codex/mI need the editEntry data entry feature to be finalised. Currently there's issues with how the data elements are displayed. Look at the available context to get an understamdnding of what's neededn an and let e me know what you dinfind. Ask me a few questions to clarify the requiremntse and let's get cracking.